extends layout

block content
  h1= title
  p Welcome to #[em All Issue Home], a very basic Express website.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The DB has the following record counts:

    ul
      li #[strong Issues:] !{data.issue_count}
